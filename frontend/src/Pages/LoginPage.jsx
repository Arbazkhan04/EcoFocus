import React from 'react';
import Login from '../Components/Auths/Login'; // Adjust the import path based on your structure

const LoginPage = () => {
    return (
        <div>
            {/* Add additional layout, if any */}
            <Login />
        </div>
    );
};

export default LoginPage;
